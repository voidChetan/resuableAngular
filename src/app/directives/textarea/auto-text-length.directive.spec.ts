import { AutoTextLengthDirective } from './auto-text-length.directive';

describe('AutoTextLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoTextLengthDirective();
    expect(directive).toBeTruthy();
  });
});
